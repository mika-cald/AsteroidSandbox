Game set up:

1. Clone the repository
2. Run npm install in git bash
3. Run npx webpack --mode development or npx webpack --watch
4. Open live server through the homepage.html
5. To run the WebSocket player count use command node ws-server.js in another terminal
